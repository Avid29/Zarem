<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Zarem.WinUI.Views.Pages.App.SettingsSubPages.AssemblerSettingsSubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:base="using:Zarem.WinUI.Views.Pages.App"
    xmlns:local="using:Zarem.WinUI.Views.Pages.App.SettingsSubPages"
    xmlns:ctc="using:CommunityToolkit.WinUI.Controls"
    xmlns:helpers="using:Zarem.WinUI.Helpers"
    xmlns:senums="using:Zarem.Services.Settings.Enums"
    mc:Ignorable="d">

    <StackPanel>
        <StackPanel.ChildrenTransitions>
            <EntranceThemeTransition FromVerticalOffset="50" IsStaggeringEnabled="True" />
        </StackPanel.ChildrenTransitions>
        
        <ctc:SettingsCard x:Uid="/Settings/AssemblerLanguageSC"
                              Header="Assembler Language"
                              Description="Set the language used by for assembler log information.">
            <ctc:SettingsCard.HeaderIcon>
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE774;" />
            </ctc:SettingsCard.HeaderIcon>
            <ComboBox SelectedItem="{x:Bind ViewModel.AssemblerLanguageOverride, Mode=TwoWay}"
                          ItemsSource="{x:Bind ViewModel.AssemblerLanguageOptions, Mode=OneWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{x:Bind base:SettingsPage.LanguageDisplayName((x:String)), Mode=OneWay}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </ctc:SettingsCard>
    </StackPanel>
</UserControl>
