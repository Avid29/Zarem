<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Zarem.IDE.Views.Pages.Settings.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctc="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Zarem.IDE.Helpers"
    xmlns:local="using:Zarem.IDE.Views.Pages.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:selectors="using:Zarem.IDE.Selectors"
    xmlns:senums="using:Zarem.IDE.Services.Settings.Enums"
    xmlns:svms="using:Zarem.IDE.ViewModels.Pages.Settings"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftGutter" Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="RightGutter" Width="auto" />
            </Grid.ColumnDefinitions>

            <ctc:Segmented
                x:Name="SegmentedPageSelector"
                Grid.ColumnSpan="3"
                Margin="8,8,8,32"
                HorizontalAlignment="Center"
                ItemsSource="{x:Bind ViewModel.SubPages}"
                SelectedIndex="0">
                <ctc:Segmented.ItemTemplate>
                    <DataTemplate x:DataType="svms:SettingsSubPageViewModel">
                        <TextBlock Text="{x:Bind Title}" />
                    </DataTemplate>
                </ctc:Segmented.ItemTemplate>
            </ctc:Segmented>

            <ContentControl
                x:Name="ContentDisplay"
                Grid.Row="1"
                Grid.Column="1"
                MaxWidth="800"
                HorizontalContentAlignment="Stretch"
                Content="{x:Bind SegmentedPageSelector.SelectedItem, Mode=OneWay}">
                <ContentControl.ContentTemplateSelector>
                    <selectors:SettingsSubPageTemplateSelector>
                        <selectors:SettingsSubPageTemplateSelector.AppPageTemplate>
                            <DataTemplate x:DataType="svms:AppSettingsViewModel">
                                <local:AppSettingsSubPage ViewModel="{x:Bind}" />
                            </DataTemplate>
                        </selectors:SettingsSubPageTemplateSelector.AppPageTemplate>
                        <selectors:SettingsSubPageTemplateSelector.AssemblerPageTemplate>
                            <DataTemplate x:DataType="svms:AssemblerSettingsViewModel">
                                <local:AssemblerSettingsSubPage ViewModel="{x:Bind}" />
                            </DataTemplate>
                        </selectors:SettingsSubPageTemplateSelector.AssemblerPageTemplate>
                        <selectors:SettingsSubPageTemplateSelector.EditorPageTemplate>
                            <DataTemplate x:DataType="svms:EditorSettingsViewModel">
                                <local:EditorSettingsSubPage ViewModel="{x:Bind}" />
                            </DataTemplate>
                        </selectors:SettingsSubPageTemplateSelector.EditorPageTemplate>
                    </selectors:SettingsSubPageTemplateSelector>
                </ContentControl.ContentTemplateSelector>
            </ContentControl>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1280" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentDisplay.MaxWidth" Value="2000" />
                        <Setter Target="LeftGutter.Width" Value="0.5*" />
                        <Setter Target="RightGutter.Width" Value="0.5*" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </ScrollViewer>
</UserControl>
