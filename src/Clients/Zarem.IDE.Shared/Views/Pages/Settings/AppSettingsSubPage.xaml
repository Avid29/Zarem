<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Zarem.IDE.Views.Pages.Settings.AppSettingsSubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctc="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Zarem.IDE.Helpers"
    xmlns:local="using:Zarem.IDE.Views.Pages.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Zarem.Models"
    xmlns:senums="using:Zarem.IDE.Services.Settings.Enums"
    mc:Ignorable="d">

    <StackPanel>
        <StackPanel.ChildrenTransitions>
            <EntranceThemeTransition FromVerticalOffset="50" IsStaggeringEnabled="True" />
        </StackPanel.ChildrenTransitions>

        <ctc:SettingsCard
            x:Uid="/Settings/AppThemeSC"
            Description="Change the app's theme."
            Header="Theme">
            <ctc:SettingsCard.HeaderIcon>
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE2B1;" />
            </ctc:SettingsCard.HeaderIcon>
            <ComboBox ItemsSource="{x:Bind ViewModel.AppThemeOptions, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.AppTheme, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="senums:Theme">
                        <TextBlock Text="{x:Bind helpers:LocalizationHelper.Localize((senums:Theme)), Mode=OneWay}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </ctc:SettingsCard>

        <!--<ctc:SettingsCard x:Uid="/Settings/RestoreOpenProjectSC"
                          Header="Restore Open Project/Folder"
                          Description="Set whether or not the app should cache and restore the open the project/folder when opened.">
            <ToggleSwitch IsOn="{x:Bind ViewModel.RestoreOpenProject, Mode=TwoWay}"/>
        </ctc:SettingsCard>-->

        <ctc:SettingsCard
            x:Uid="/Settings/AppLanguageSC"
            Description="Set the app's display language. (Requires a restart to take effect)."
            Header="App Language">
            <ctc:SettingsCard.HeaderIcon>
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE774;" />
            </ctc:SettingsCard.HeaderIcon>
            <ComboBox ItemsSource="{x:Bind ViewModel.AppLanguageOptions, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.LanguageOverride, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{x:Bind local:SettingsPage.LanguageDisplayName((x:String)), Mode=OneWay}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </ctc:SettingsCard>

        <ctc:SettingsCard
            x:Uid="/Settings/AboutSE"
            Command="{x:Bind ViewModel.OpenAboutCommand, Mode=OneWay}"
            Description="© Avishai Dernis"
            Header="Zarem"
            IsClickEnabled="True">
            <ctc:SettingsCard.HeaderIcon>
                <ImageIcon Source="{StaticResource AppIconSmall}" />
            </ctc:SettingsCard.HeaderIcon>
            <ctc:SettingsCard.Content>
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    IsTextSelectionEnabled="True"
                    Text="{x:Bind ViewModel.AppVersion, Mode=OneWay}" />
            </ctc:SettingsCard.Content>
        </ctc:SettingsCard>
    </StackPanel>
</UserControl>
